import '../styles/Desc.css'
import { React } from "react"

const Desc = () => {
    return (
        <div id = "desc">
            RPS Online Language Compository
        </div>
    );
}
export default Desc;